import 'package:flutter/cupertino.dart';
import 'package:videop/mtab.dart';
import 'home.dart';

main() => runApp(myApp());

// ignore: camel_case_types
class myApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return mTabBar();
  }
}
